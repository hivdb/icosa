import {describe} from 'vitest';

// Test suite temporarily disabled due to unresolved ramda subpath
// resolution issues under vitest. Re-enable when dependency is fixed.
describe.skip('hiv view modules', () => {});
