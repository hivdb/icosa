import type Loader from '../loader';

/**
 * Props for the FixedLoader component.
 * Inherits all props from the Loader component.
 */
export type FixedLoaderProps = React.ComponentProps<typeof Loader>;
